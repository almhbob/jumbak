[phases.setup]
nixPkgs = ["nodejs_20", "pnpm"]

[phases.install]
cmds = ["pnpm install --frozen-lockfile=false"]

[phases.build]
cmds = ["pnpm --filter @jumbak/server build"]

[start]
cmd = "pnpm --filter @jumbak/server start"
