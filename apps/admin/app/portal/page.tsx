'use client';
import {FormEvent,useState} from 'react';
type Role='operations'|'developer'|'business';
type Lang='ar'|'en';
const roles={operations:{ar:'إدارة التطبيق',en:'App management',descAr:'الرحلات، السائقين، الدعم، ومهام التشغيل اليومية.',descEn:'Rides, drivers, support, and daily operating tasks.',path:'/operations',hash:'2ac649458afdeef8385fe919f1f5262e1e025769fd823c12704e04e5b7c424f8'},developer:{ar:'حساب المطور',en:'Developer account',descAr:'المدن، المناطق، المركبات، الأسعار، والربط التقني.',descEn:'Cities, zones, vehicles, pricing, and technical integrations.',path:'/settings',hash:'13ce58d682fcdeaebf049a8db37bc77f5441a3d83a8a8a106c0c97e163fc40a0'},business:{ar:'الإدارة والاتفاقات',en:'Business and agreements',descAr:'الاشتراكات، نسب الأرباح، العقود، وروابط السداد.',descEn:'Subscriptions, profit share, agreements, and payment links.',path:'/business',hash:'2ac649458afdeef8385fe919f1f5262e1e025769fd823c12704e04e5b7c424f8'}} as const;
const copy={ar:{title:'بوابة دخول Jnbk جنبك',sub:'رابط موحد لكل لوحات العمل مع تسجيل دخول منفصل حسب الصلاحية.',password:'كلمة السر',login:'دخول',bad:'كلمة السر غير صحيحة لهذه الصلاحية',choose:'اختر لوحة العمل',toggle:'English',home:'الرئيسية',note:'كل صلاحية تفتح مساحة عمل مختلفة وتحفظ الجلسة داخل المتصفح.',show:'إظهار',hide:'إخفاء'},en:{title:'Jnbk Unified Portal',sub:'One link for all workspaces with separate login per role.',password:'Password',login:'Login',bad:'Invalid password for this role',choose:'Choose workspace',toggle:'العربية',home:'Home',note:'Each role opens a different workspace and stores its session in the browser.',show:'Show',hide:'Hide'}};
async function sha256(v:string){const b=await crypto.subtle.digest('SHA-256',new TextEncoder().encode(v));return Array.from(new Uint8Array(b)).map(x=>x.toString(16).padStart(2,'0')).join('')}
export default function Portal(){const params=new URLSearchParams(typeof window==='undefined'?'':window.location.search);const initial=(params.get('lang')==='en'?'en':'ar') as Lang;const[lang]=useState<Lang>(initial);const[role,setRole]=useState<Role>('operations');const[pw,setPw]=useState('');const[showPw,setShowPw]=useState(false);const[err,setErr]=useState('');const t=copy[lang];const ar=lang==='ar';async function login(e:FormEvent){e.preventDefault();const selected=roles[role];if(await sha256(pw)===selected.hash){sessionStorage.setItem(`jnbk_${role}_auth`,'true');if(role==='developer')sessionStorage.setItem('jnbk_dev_auth','true');window.location.href=`${selected.path}?lang=${lang}`}else setErr(t.bad)}return <main dir={ar?'rtl':'ltr'} style={{textAlign:ar?'right':'left'}}><section className='hero'><div className='heroTop'><div><p className='kicker'>Jnbk جنبك</p><h1>{t.title}</h1><p>{t.sub}</p></div><div className='topActions'><a className='languageSwitch' href='/'>{t.home}</a><a className='languageSwitch' href={`/portal?lang=${ar?'en':'ar'}`}>{t.toggle}</a></div></div></section><section className='panel'><h2>{t.choose}</h2><p className='muted'>{t.note}</p><div className='grid settingsGrid'>{(Object.keys(roles) as Role[]).map(k=><button key={k} className='card buttonReset' onClick={()=>setRole(k)} style={{border:role===k?'2px solid #D6A936':'0',textAlign:ar?'right':'left'}}><p>{ar?roles[k].ar:roles[k].en}</p><strong>{k}</strong><p className='muted'>{ar?roles[k].descAr:roles[k].descEn}</p></button>)}</div>{err?<div className='notice error'>{err}</div>:null}<form className='formGrid' onSubmit={login}><div style={{display:'flex',gap:8,alignItems:'stretch'}}><input style={{flex:1}} type={showPw?'text':'password'} value={pw} onChange={e=>setPw(e.target.value)} placeholder={t.password}/><button className='languageSwitch buttonReset' type='button' onClick={()=>setShowPw(!showPw)} style={{color:'#063B63',background:'#F7FAFC',border:'1px solid #D9E2EC'}}>{showPw?t.hide:t.show}</button></div><button className='primaryAction' type='submit'>{t.login} - {ar?roles[role].ar:roles[role].en}</button></form></section></main>}
